<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cursopluralsight.bruna.loginestudo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/apple1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".telalogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".superMarket" />
        <activity android:name=".terceiratela" />
        <activity android:name=".telacadastrar" />
        <activity android:name=".telacadastrar2"></activity>
    </application>

</manifest>